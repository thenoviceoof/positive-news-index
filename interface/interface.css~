/* header */
header { padding-left:5%;border-bottom:10px #c0c0c0 solid;
	 padding-top:50px;
	 width:100%; }
h1 { font-size:2em; }

/* container */
div { display:block;margin:1em;
      margin-left:10%; width:100%; padding:0; }
ul { display:block;width:60em;background-color:#909090;padding:2px; }

/* li */
li { margin:4px;padding:0.5em;
     display:block; }
section { display:inline-block; }
h2 { display:inline-block;margin:0.25em;
     font-size:1.4em; }
details { background-color:#e0e0e0;border:1px black solid;
	  padding:0.45em;vertical-align:auto;float:left;width:3em;}
div.buttons { margin:0;padding:0;display:inline-block;width:auto; }
code { display:inline-block; margin:0 1em; }
code button { background-color:#70ff70; }
code button + button { background-color:#ff7070; }
code+code button+button { background-color:#707070; }
code+code button+button+button { background-color:#ff7070; }
article { display:none; }

/* telling us which is which */
.hi { background-color:#b0ffb0; }
.md { background-color:#c0c0c0; }
.lo { background-color:#ffb0b0; }

/* footer */
footer { margin-top:2em;padding-left:70%;padding-top:0.4em;
	 width:100%;
	 border-top:3px gray solid; }